public class Main {
    public static void main(String[] args) {
        // =================================================================
        // Практика #1: Тривиальные бесконечные циклы (закомментированы, чтобы код не завис)
        // =================================================================

        /*
        // Бесконечный цикл while
        while (true) {
            System.out.println("Этот цикл while никогда не остановится сам");
        }
        */

        /*
        // Бесконечный цикл do-while
        do {
            System.out.println("Этот цикл do-while выполнится один раз и станет бесконечным");
        } while (true);
        */


        // =================================================================
        // Практика #2: Примеры использования break и continue в цикле for
        // =================================================================
        System.out.println("--- Демонстрация break и continue ---");

        // Выведем нечетные числа от 1 до 10, но остановим цикл, если дойдем до 7
        for (int i = 1; i <= 10; i++) {

            // Если число четное — пропускаем его с помощью continue
            if (i % 2 == 0) {
                continue; // Код ниже в этой итерации не выполнится, переходим к i++
            }

            // Если дошли до 7 — полностью прерываем цикл с помощью break
            if (i == 7) {
                System.out.println("Встретили число 7. Прерываем цикл с помощью break.");
                break; // Цикл полностью завершает работу
            }

            // Этот код сработает только для нечетных чисел меньше 7
            System.out.println("Текущее нечетное число: " + i);
        }

        System.out.println("Программа успешно завершена после выхода из цикла.");
    }
}
